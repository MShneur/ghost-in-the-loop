name: Worker 2 Claude Native Build (historical carrier)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  historical-carrier:
    runs-on: ubuntu-latest
    steps:
      - name: Historical carrier is intentionally inert
        run: echo 'Claude native takeover promotion completed and was independently audited; this one-shot carrier no longer mutates the repository.'
