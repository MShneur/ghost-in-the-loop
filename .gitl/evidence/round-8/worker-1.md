# Ghost Worker Evidence

## Identity

- Round: 8
- Worker evidence slot: 1
- Intended role: Supervisor / integrator
- Executed by successor: `scheduled-worker-6-r8-docs-supervisor-plan-05`
- Assignment: `R8-SUPERVISOR-DOCS-RECONCILIATION-PLAN`
- Program: `DOCS-RECONCILIATION`
- Isolated branch: `agent/8.8-repair-resume`
- Inspected handoff head: `c3cd41b89914e3a5a9f299c57f2d271973100567`
- Lease claim commit: `7bf75807870f3a7dc5b204194de0766eb695508b`
- Canonical maker: Personal-Forge `CHATGPT_AUTOMATION_MAKER.md`, v1.1.

## State Read

The bounded Round-4 and Round-5 certifications remain accepted only with their recorded limits. Round-6 structural work remains accepted only at deterministic/hosted ChatGPT+Claude cross-adapter scope; exact current live ChatGPT/Claude insertion and physical Android/WebView/GeckoView, real IME/browser-toolbar, real assistive-technology and calibrated low-end-device claims remain uncertified. Read-only live inspection remains authorized when a functioning carrier exists.

Round-7 BUILD-IDENTITY is accepted only at bounded immutable-payload/provenance-and-test-binding/candidate-channel/non-published scope. Candidate `8.8.0` is not publication identity. Stable userscript install/update authority remains `main`, observed at `8.7.1` during the identity audit, and `publishReady` remains false.

Delivery-Pressure selects documentation reconciliation over research. The canonical counter is zero research-only wakes since the latest product/test/package artifact and executable docs work exists.

## Step Performed

Performed a docs-only supervisor inventory and opened the smallest dependency-safe reconciliation chain. No product code, generated payload, package/dependency, version, channel, Send/CHOICE/route/lease/uncertainty behavior, publication object, or release state was changed.

## Documentation Drift Inventory

### 1. README.md — user-facing / contributor-facing

- The Install badge and Tampermonkey install path correctly target `main`; that stable-channel link must **not** be rewritten to the isolated candidate while publication is unauthorized.
- README describes current safety concepts well (reviewed Send authority, at-most-once confirmation, uncertainty pause), but it does not expose the accepted 8.8 candidate-versus-stable/non-published identity boundary or the bounded R4/R5/R6/R7 certification limits that final release documentation must preserve.
- README's contributor rule says **every push** must update `DEVLOG.md` and `CHANGELOG.md`. That is contradicted by the current autonomous control plane, which intentionally makes coordination/evidence-only `.gitl` commits. The rule needs a docs-only clarification so product/release-facing changes remain documented without falsely requiring history churn for every coordination commit.
- The contributor footer still spotlights `Release 8.3.0` and an editor attribution. It is historical text, not current release authority, but in a release-facing section it can be misread as the current release identity and should be made explicitly historical or removed.

### 2. docs/ARCHITECTURE.md — maintainer-facing authority

- The architecture correctly states that `ghost-in-the-loop.user.js` is canonical and `extension/content.js` is generated, and it preserves reviewed-actuator/at-most-once Send rules.
- It still labels several sections with older version anchors (`v8.2.0`, `v8.3`, `v8.7`) and contains no release-candidate identity/provenance contract, no Round-6 adapter-owned structural-mount authority order (specialized -> standard -> rail fallback), and no explicit statement of current live/physical certification limits.
- Its test-engine description says real-site DOM remains manual and correctly distinguishes desktop Firefox from Android GeckoView; the docs pass must preserve that limitation rather than turning hosted fixture success into live certification.

### 3. CHANGELOG.md — release-facing history

- The top `8.8.0` section covers workflow-neutral controls, CHOICE, Repair & Resume, and Advanced-mode decisions, but it does not yet reconcile the later 8.8 release-candidate work that is now durable: bounded lifecycle certification, Long-Chat grouped-selector work and its explicit linear/send-observation limits, bounded ChatGPT+Claude structural shell work, BUILD-IDENTITY, and non-publication status.
- Any new 8.8 changelog text must distinguish implemented behavior from certification scope and must not imply that 8.8 is already published to `main`, a tag, Release, or store.

### 4. .gitl/orchestration/README.md — maintainer control-plane drift

- The authoritative state/plan run in `continuous-local-human-gates` with automatic earliest-ready succession and local human gates.
- The orchestration README still says `review-after-round` is the current mode, that all tasks no-op after Worker 6, and that continuous mode is merely future. That prose is now directly inconsistent with canonical state, the user directive, and the succession rule.
- The README also says the supervisor is the only role that may change the roadmap, while the canonical succession policy now allows any eligible successor to claim a missed supervisor assignment. The intended **supervisor function** remains roadmap authority; timer identity is not ownership. This distinction needs explicit reconciliation.

### 5. DEVLOG.md and focused docs

- DEVLOG is large historical reasoning context and should receive only a concise release-candidate reconciliation note if the docs implementation finds a concrete contradiction; it should not be rewritten wholesale.
- `docs/lifecycle-recovery-contract.md`, `docs/REPAIR_RESUME_*`, and mobile/skin documents are narrower references. The implementation should touch them only when a concrete contradiction with the accepted bounded evidence is found; breadth alone is not justification.

## Claim-Limit Ledger to Preserve

1. **Round 4 lifecycle:** accepted bounded deterministic/hosted contract; synthetic freeze/resume/discard fixtures and Pixel-class emulation are not physical browser/device discard or scheduler proof.
2. **Round 5 Long Chat:** grouped-selector answer collection reduces redundant returned-match work at the tested retained-DOM envelope, but lookup remains history-linear; the 8004-match/sample Send-observation path remains an explicit limit; hosted p95 is not a calibrated physical-hardware budget; physical Android and GeckoView remain uncertified.
3. **Round 6 mobile shell:** accepted only at deterministic/hosted ChatGPT+Claude cross-adapter scope. Exact current live ChatGPT/Claude insertion remains UNKNOWN/not certified. Specialized structural authority requires adapter-owned site/signature verification and demotes to standard/rail on uncertainty; it does not inherit Send authority.
4. **Round 7 build identity:** candidate `8.8.0` is an isolated non-published candidate. Immutable payload hashes plus provenance/test bindings define candidate identity; `.gitl`-only head movement is not a new payload. Stable install/update remains `main`; no publication identity exists yet.
5. **Dependency audit:** `brace-expansion@1.1.15` and `js-yaml@3.14.2` remain real high-severity indirect nodes in the Jest development graph. `npm audit --omit=dev` found zero vulnerabilities and no concrete path into immutable shipped payload was established. Shipped exploitability remains UNKNOWN / not claimed; no blind upgrade is authorized.
6. **Hosted timing dissent:** preserve red and green ordinary-CI observations without weakening the accepted Round-5 thresholds or calling timing-only variance a product regression.
7. **Publication:** `publishReady=false`; no main merge, tag, GitHub Release, store upload, publication URL change, or stable-channel promotion is authorized.

## New Timing Evidence Discovered During This Supervisor Wake

A later ordinary CI result completed after the Round-7 A4 audit evidence was written and therefore is not represented in that audit document:

- A4 lease-claim head: `012012dab52408f4a0a51b73d8a785602f661897`.
- Compare from the previously green pre-audit head `75b0a7c9aedce61f09b953d3f452e832a03d4fa7` to that claim head changes only `.gitl/autopilot-state.json`; product and test semantics are identical.
- Ordinary CI run `31251250525`: unit/base/identity job `93087860958` succeeded; E2E job `93087860928` failed.
- E2E artifact `9020109219`, SHA-256 `8366016a7101945cfefab0ed2c66e721b81cb9793f48194c7bab012538022f40`.
- Artifact inspection found one failed test: the preserved Chromium Round-5 A2 grouped-selector timing oracle. One execution missed the large p95 `<2.3 ms` gate at about `2.7 ms`; retry missed the small p95 `<=0.875 ms` gate at about `1.6 ms`. No product/safety assertion was implicated.

Classification: additional **hosted timing reproducibility evidence**, not a demonstrated product regression and not permission to weaken the existing numerical oracle. Documentation and FINAL-CERT-PACKAGE must carry this newer red observation alongside the earlier green/red/flaky observations rather than selecting only favorable runs.

The ordinary CI run triggered by this supervisor lease claim was still in progress when this planning evidence was written; it is not treated as PASS or FAIL.

## Bounded Docs Roadmap

### R8-A1-DOCS-RECONCILE — ready

Docs-only implementation. Reconcile `README.md`, `docs/ARCHITECTURE.md`, `CHANGELOG.md`, and `.gitl/orchestration/README.md`; inspect `DEVLOG.md` and focused docs and edit them only for concrete contradictions. The implementation must preserve stable-main install URLs, candidate non-publication, all bounded R4-R7 limits, dependency disposition, timing dissent, and every fail-closed safeguard. No product/generated/dependency/version/channel semantics may change.

Required validation is documentation-focused: inspect the exact diff, verify links/paths and version/channel statements against repository sources, verify no prohibited product/payload files changed, and record any ordinary CI produced by the docs commit without treating hosted timing variance as permission to weaken tests.

### R8-A2-DOCS-REDTEAM-AUDIT — backlog until A1 submitted

Independent Devil's-Advocate docs audit. Falsify overclaims by tracing each release-facing statement back to R4/R5/R6/R7 evidence and current repository identity. It must reject any text that implies physical/live certification, stable publication, resolved shipped exploitability, asymptotically bounded Long-Chat lookup, or universal timing reproducibility. It must also verify that README's stable install link still targets `main` and that control-plane docs no longer contradict current succession/local-human-gate policy.

On acceptance, A2 may close DOCS-RECONCILIATION at bounded documentation-consistency scope and expose FINAL-CERT-PACKAGE planning; it may not itself publish, tag, merge, or create a release.

## Tests / CI

- Product tests newly run by this planning assignment: **none**. This is a planning/evidence-only assignment; no product/test-semantic mutation was made.
- Ordinary CI is triggered automatically by coordination commits and is evidence, not a prerequisite for the planning inventory unless a product/test-semantic change occurs.
- The latest completed post-A4 claim-head run described above is retained as timing dissent, not hidden.

## Acceptance Criteria

- Documentation drift inventoried: PASS.
- Candidate-versus-stable/non-published identity preserved: PASS.
- R4/R5/R6/R7 claim limits preserved: PASS.
- A2 dependency disposition and timing dissent carried forward: PASS.
- Smallest dependency-safe docs-only implementation + independent audit chain defined: PASS.
- FINAL-CERT-PACKAGE kept required but not activated before docs audit: PASS.
- Product behavior or safety authority changed: NO.
- Publication action taken: NO.
- `publishReady` changed to true: NO.

## Risks and Limitations

- Documentation can create a release-safety failure even without changing code if it overstates live/physical certification or accidentally treats the isolated candidate as the stable channel. A1/A2 must treat these as falsifiable release claims, not copy-editing preferences.
- The hosted timing oracle has repeated same-payload red/green variance. The docs pass must preserve the numerical acceptance threshold and the variance record; it should not claim either universal regression or universal stability.
- The dependency audit narrows the two high findings to the development graph with no concrete shipped path, but it does not prove universal non-exploitability.
- Current live ChatGPT/Claude structural capture is still not obtained. Its absence remains a live-binding certification limit, not a reason to block docs reconciliation or deterministic release-path work.

## Handoff

Mark `R8-SUPERVISOR-DOCS-RECONCILIATION-PLAN` submitted, make `R8-A1-DOCS-RECONCILE` the earliest ready assignment, keep `R8-A2-DOCS-REDTEAM-AUDIT` dependency-blocked, keep research temporarily ineligible, and keep `FINAL-CERT-PACKAGE` backlog until the docs audit closes this program.
