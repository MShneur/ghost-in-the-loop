# User Directive — Release Pressure / Anti-Loop

**Date:** 2026-08-07
**Authority:** explicit user direction in interactive ChatGPT session
**Applies to:** Ghost in the Loop 8.8 shared autonomous chain

## Problem statement

The current chain has produced substantial research around the Round-6 live-structure blocker but is over-weighting analysis relative to testable development. The user does not want the workers to remain in a loop where each wake finds another research angle while no new product/test/package artifact is produced.

The team must behave like a delivery team pushing a release candidate, not like an open-ended research seminar.

## Human/deferred decisions resolved by delegated expert review

The user explicitly delegated these ordinary engineering decisions to the expert/persona chain and asked it to choose the best evidence-backed option.

### DQ-R4-LIFECYCLE-REVIEW

**Decision:** accept the bounded Round-4 lifecycle certification.

Preserve all explicit limits. Do not claim physical Android discard/freeze, GeckoView/Firefox-Android lifecycle certification, actual browser scheduler freeze/discard certification, or calibrated low-end hardware performance unless separately proven.

### DQ-R5-LONGCHAT-REVIEW

**Decision:** accept the bounded Round-5 Long Chat certification.

Preserve all explicit limits. The history-linear Send observation path and lack of physical Android/GeckoView certification remain documented limits, not silent PASS claims.

### DQ-R6-LIVE-STRUCTURE-CAPTURE

**Decision:** read-only live authenticated ChatGPT inspection is authorized when a functioning interactive browser carrier is available.

The probe remains read-only: no typing, click, submit, input, keydown, Send actuation, Send replacement, or Send interception.

## Development directive

The absence of live-host structural certification must no longer be treated as a blanket reason to avoid creating testable product artifacts.

The live capture remains a **certification and adapter-binding gate**, but the chain should now open the smallest reversible, fail-closed structural prototype that can be exercised against deterministic fixtures and existing reviewed adapter assumptions.

On the next eligible wake, reconcile the plan/state so that Round-6 moves from research-only behavior toward an executable prototype path. The preferred action is to make `R6-A2-MOBILE-SHELL-BUILD` ready for a **prototype-only** implementation under these constraints:

- isolated branch only;
- no main/merge/tag/publish/release;
- preserve exact Send identity and zero unintended Send actuation;
- preserve CHOICE/route/lease/uncertainty fail-closed behavior;
- structural verification failure must fall back cleanly to the existing rail behavior;
- no claim of live ChatGPT structural certification until A1X evidence is actually obtained;
- tests must make unsafe structural choices fail visibly rather than hiding them with fallback success;
- implementation should be the smallest falsifiable candidate, not a broad framework rewrite.

If a worker concludes even a prototype cannot safely be built, it must identify the exact invariant that makes code impossible and demonstrate why a deterministic fixture cannot exercise it. “Live capture is missing” by itself is no longer sufficient.

## Delivery-Pressure Checkpoint

Adopt the Personal-Forge canonical maker v1.1 Delivery-Pressure Checkpoint immediately.

A checkpoint is due when any of these are true:

1. 12 hours have elapsed since the previous delivery review while 8.8 remains active;
2. six consecutive wakes have been research-only since the last product/test/package artifact;
3. repeated research keeps circling the same blocker without changing the executable path.

At each checkpoint, use at least supervisor/release, builder, independent test, Red Team/devil's advocate, and user/usability lenses.

The checkpoint must answer:

- What can be built or tested now?
- Which missing evidence limits certification rather than development?
- What is the smallest falsifiable artifact?
- Is another research wake more informative than implementing/testing a candidate?
- Are workers adding release scope rather than completing it?

If a safe reversible artifact can be produced, research fallback is temporarily ineligible until at least one implementation/test/package attempt is made and recorded.

## Release-push expectation

Once the mobile-shell implementation/certification chain is complete, advance promptly through BUILD-IDENTITY, DOCS-RECONCILIATION, FINAL-CERT-PACKAGE, and SHA-256 verification.

Do not add new release-critical requirements merely because more research is possible. Explicitly document unproven claims as limits and move on when they are not necessary for the declared 8.8 release scope.

Publication remains forbidden until separately authorized.

## Required next-state reconciliation

The next worker with a safe lease should:

1. read canonical maker v1.1 and this directive;
2. mark R4 and R5 human reviews accepted at bounded scope using delegated user authority;
3. mark R6 read-only inspection authorized, while leaving the actual live capture UNKNOWN until obtained;
4. add Delivery-Pressure state fields/counters if absent;
5. convert the Round-6 path from indefinite research-only fallback to the smallest prototype/build attempt that can be safely tested;
6. keep live-host certification as a later hard PASS requirement before release claims that depend on it;
7. preserve every fail-closed safeguard and all protected publication boundaries.