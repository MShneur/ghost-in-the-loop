const fs=require('fs'),path=require('path');const src=fs.readFileSync(path.join(__dirname,'..','ghost-in-the-loop.user.js'),'utf8');describe('v9 composer text verification',()=>{test('textarea/input verification reads value before empty innerText',()=>{expect(src).toContain("typeof el.value === 'string'");expect(src).toContain("/^(TEXTAREA|INPUT)$/.test");});test('does not change Send authority',()=>{expect((src.match(/button\\.click\\(\\)/g)||[]).length).toBe(1);expect(src).not.toContain('requestSubmit(');});});